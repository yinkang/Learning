#############################################
Develop Note
Reference to 曾经埋的坑与亲手开发的bug
Start with "---" means need attention.
#############################################

1.给一个API加参数的时候,需要考虑在上线时,不是所有的server都是同时上的情况,所以应该允许增加的参数为空,然后对空进行异常判断。